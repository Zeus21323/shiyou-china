# 山水界面与地理呈现

宣纸底色 #f1eee3、墨色 #293f37、石绿 #467567、深绿 #2c5b50、旧金 #947340、星夜 #102c31。楷体负责题签与诗文，系统中文字体负责操作说明；地图为主画，右栏为阅读行笺。

地图纹理不使用生成式山景拼贴。Mapzen Terrarium 高程按经纬度重采样，依据局部起伏与坡度生成青绿皴染。全国 34 个省级区域均有独立配准的纹理、DEM 和精细三维网格，统一垂直夸张为艺术展示而非测绘比例。全国初始视野共享概览网格和纹理；选中省份时按需加载本省资源，加载期间保留基础地图，失败提供重选提示。细节到达后同时渐变纹理、高程、法线和标签锚点；取消选择反向过渡；最近 3 个模型通过带使用计数的 LRU 缓存保留，淘汰时释放 GPU 资源。相机缩放和平移不依赖资源是否加载成功。生成网格：node --experimental-strip-types scripts/build-terrain-mesh.mjs。

数据来源、逐省范围、纹理采样间隔、数值网格间隔与模型大小见 public/data/terrain/manifest.json。每省按实际范围自适应分辨率，包含离岛；海南等离岛跨度较大的地区按主体陆块确定网格细分密度。纹理采样间隔不是源数据精度保证，DEM 数值网格为纹理网格的每 4 点采样。源瓦片的孤立尖峰/深坑采用 3×3 局部中位数、至少 250m 且超过 8 MAD 的阈值清理；不裁掉连续的负高程洼地，原始瓦片仍留存。Natural Earth 水系为 1:1000 万概化主要河湖，不含完整支流与小湖。它是依据真实地形的艺术化地图，不是地质岩性图或导航测绘成果。生成脚本为 scripts/build-geographic-terrain.py；公开源瓦片缓存在忽略目录 work/terrain，生成支持恢复。

交互保留 5A 优先、逐级展开、标签避让、真实坐标、作品阅读。筛选现含湿地和森林公园，保留171个景区、100个坐标、18篇作品。左键与单指平移、方向键平移；滚轮使用累积目标和连续插值，并以指针为缩放中心。双指缩放使用相同目标。省级缩小到初始视野60%只取消选择，保持当前缩放和中心；只有明确点击省份或全国视野按钮才重新适配镜头。滚轮放大、触屏放大与拖动期间每65ms检查视野，候选保持70ms后切换选择，不等待镜头停止。进入阈值为省级适配比例64%，退出为60%，全国概览不强选；缩小时不自动重新选省。中心命中真实省界优先，中心在近海时参考周围4个样点，全海面不选省。缩放数字统一相对于全国基准，不随取消选择而跳变。4A展开阈值1.7、收起1.5，避免边界反复切换。标签保持相对偏移并淡入淡出，静止时跳过布局，最高30次每秒。地图和星河保留各自实例，以透明度过渡切换，隐藏场景暂停帧循环。未宣称所有设备恒定60 FPS；双指操作尚未手机实机测试。

省名标签引导线不超过50px，位置过密则留在省份选择列表，避免长线误导。引导线与标签在同一个坐标和动画容器，端点取牌身最近边缘，3D旋转以该连接点为轴。未选择时全国使用同一纹理口径，任意省份被选中都可显示其独立精细纹理；高亮色和省界强调仅跟随当前选择。

地址数据 public/data/scenic-addresses.json 通过原有已核验高德POI ID逐一取回，97条有地址，覆盖全部10个诗文目的地。联合景区明确为代表点地址，其余74个不猜测地址，显示所在市区和“详细地址待核验”。可用 scripts/refresh-scenic-addresses.mjs 更新。景区数据仍仅覆盖浙江，跨省高亮不会串用浙江景点，其他省份显示待接入。

体验审查落实：44px 地图标签与控制、键盘焦点、跳转内容入口、搜索标签、移动端布局、长列表延迟渲染、减少动态效果。浏览器检查全国到浙江及诗文阅读；布局自动测试覆盖密集标签与聚合优先级。仍可继续改善 URL 深链接与极端小屏幕体验。

城市标注来自 Natural Earth populated places：34 个省级地区的中心城市加深圳，共35个去重地名；一线城市采用北上广深口径。城市横排题签与省份/景区牌共同避让，44px点击区域、引导线不超过50px；不同城市不会与景区聚合。来源和概化坐标记录见 public/data/city-labels.json，生成脚本 scripts/build-city-labels.mjs。

地形预载在滚轮指向、即将进入省级范围、悬停及键盘聚焦时启动，最多2个后台预载；预载与选中共用加载任务。高亮颜色、省界、纹理与高程使用同一进度，周边省份淡出也跟随该进度。首次冷加载仍受网络与GPU准备时间影响，加载中显示状态；失败可重选重试，不承诺零等待。
