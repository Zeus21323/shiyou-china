# 山水界面与地理呈现

宣纸底色 #f1eee3、墨色 #293f37、石绿 #467567、深绿 #2c5b50、旧金 #947340、星夜 #102c31。楷体负责题签与诗文，系统中文字体负责操作说明；地图为主画，右栏为阅读行笺。

地图纹理不使用生成式山景拼贴。Mapzen Terrarium 高程按经纬度重采样，依据局部起伏与坡度生成青绿皴染。地形已使用真实 DEM 细分为三维网格（101574 顶点、177862 三角形），同一高度比例保持平原与山地差异，垂直夸张为艺术展示而非测绘比例。浙江始终使用较细网格，不在点击时重建模型或加载新纹理。各省共享 GPU 顶点与索引缓冲。生成网格：node --experimental-strip-types scripts/build-terrain-mesh.mjs。

数据来源、范围与网格采样间隔见 public/data/terrain/manifest.json。采样间隔不是源数据精度保证；全国是概览级地形，浙江更细。Natural Earth 水系为 1:1000 万概化主要河湖，不含完整支流与小湖。它是依据真实地形的艺术化地图，不是地质岩性图或导航测绘成果。生成脚本为 scripts/build-geographic-terrain.py；公开源瓦片缓存在忽略目录 work/terrain。

交互保留 5A 优先、逐级展开、标签避让、真实坐标、作品阅读。筛选现含湿地和森林公园，保留171个景区、100个坐标、18篇作品。左键与单指平移、方向键平移；滚轮使用累积目标和连续插值，并以指针为缩放中心。双指缩放使用相同目标。省级用户缩小到初始视野60%自动回全国，进入省份的镜头运动不会误触发。4A展开阈值1.7、收起1.5，避免边界反复切换。标签保持相对偏移并淡入淡出，静止时跳过布局，最高30次每秒。地图和星河保留各自实例，以透明度过渡切换，隐藏场景暂停帧循环。未宣称所有设备恒定60 FPS；双指操作尚未手机实机测试。

体验审查落实：44px 地图标签与控制、键盘焦点、跳转内容入口、搜索标签、移动端布局、长列表延迟渲染、减少动态效果。浏览器检查全国到浙江及诗文阅读；布局自动测试覆盖密集标签与聚合优先级。仍可继续改善 URL 深链接与极端小屏幕体验。
