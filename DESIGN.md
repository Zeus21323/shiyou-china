# 山水界面与地理呈现

倾斜视图的地形表达以真实 DEM 为准：全国和34省网格统一以0.00023地图单位/米重建顶点与法线（艺术夸张），加强斜向主光、减弱环境填光，呈现真实坡面的迎光与背光差异。青绿绘画纹理继续按经纬度贴合网格，不添加随机山块。沿海底座减薄至0.10地图单位，避免侧壁厚度盖过地貌。等范围采样对照：杭嘉湖平原约8米起伏、富阳丘陵约418米、临安山区约1107米；这些是现有概化DEM的样区值，不代表整座城市。地形资源URL带高程比例版本，防止旧模型缓存与新标签高程错配。

相邻省份共边按端点坐标去重，只绘制一次；选中省份接管共边，高亮与全国省界使用同一份线段。共用顶点从同一份逐帧高程缓存取值，边界更新排在全部地形过渡之后；静止时跳过重复更新。普通省界为白色实线，选中省界保持旧金强调。城市名19px、圆点10px，北京红星27px、名称18px；标签避让宽度同步增大。

全国基准6倍为统一高亮门槛：低于6倍显示省名；达到6倍后显示鼠标所指省份的高亮、城市及景点。点击省份时镜头目标至少为6倍，飞入过程中6倍前仍显示省名；缩小至6倍以下只退出选择，不重置镜头，也不截断滚轮目标。鼠标指向持续有效直到离开地图，32ms检查、候选连续40ms后切换；跨界需移出旧省份约3px邻域，使用空间滞回避免边界反复选择。命中使用固定全国DEM，鼠标与相机未移动时复用落点，不随精细地形加载或高亮动画变化；鼠标指向优先于视野中心，包括鼠标静止和指向海面。触屏与无鼠标指向时保留视野中心策略。镜头过渡结束时落到精确目标，避免目标6倍却停在略小于6倍。canvas保留最近24次选择变化的有限诊断记录，便于区分状态切换和纯渲染闪烁。

宣纸底色 #f1eee3、墨色 #293f37、石绿 #467567、深绿 #2c5b50、旧金 #947340、星夜 #102c31。楷体负责题签与诗文，系统中文字体负责操作说明；地图为主画，右栏为阅读行笺。

地图纹理不使用生成式山景拼贴。Mapzen Terrarium 高程按经纬度重采样，依据局部起伏与坡度生成青绿皴染。全国 34 个省级区域均有独立配准的纹理、DEM 和精细三维网格，统一垂直夸张为艺术展示而非测绘比例。全国初始视野共享概览网格和纹理；选中省份时按需加载本省资源，加载期间保留基础地图，失败提供重选提示。细节到达后同时渐变纹理、高程、法线和标签锚点；取消选择反向过渡；最近6个模型通过带使用计数的LRU缓存保留，淘汰时释放GPU资源。相机缩放和平移不依赖资源是否加载成功。生成网格：node --experimental-strip-types scripts/build-terrain-mesh.mjs。

数据来源、逐省范围、纹理采样间隔、数值网格间隔与模型大小见 public/data/terrain/manifest.json。每省按实际范围自适应分辨率，包含离岛；海南等离岛跨度较大的地区按主体陆块确定网格细分密度。纹理采样间隔不是源数据精度保证，DEM 数值网格为纹理网格的每 4 点采样。源瓦片的孤立尖峰/深坑采用 3×3 局部中位数、至少 250m 且超过 8 MAD 的阈值清理；不裁掉连续的负高程洼地，原始瓦片仍留存。Natural Earth 水系为 1:1000 万概化主要河湖，不含完整支流与小湖。它是依据真实地形的艺术化地图，不是地质岩性图或导航测绘成果。生成脚本为 scripts/build-geographic-terrain.py；公开源瓦片缓存在忽略目录 work/terrain，生成支持恢复。

交互保留 5A 优先、逐级展开、标签避让、真实坐标、作品阅读。筛选现含湿地和森林公园，保留171个景区、100个坐标、18篇作品。左键与单指平移、方向键平移；滚轮使用累积目标和连续插值，并以指针为缩放中心。双指缩放使用相同目标。只有明确点击省份或全国视野按钮才重新适配镜头；自动选省统一采用6倍门槛。无鼠标时中心命中真实省界优先，中心在近海时参考周围4个样点，全海面不选省。缩放数字统一相对于全国基准，不随取消选择而跳变。4A展开阈值1.7、收起1.5，避免边界反复切换。标签保持相对偏移并淡入淡出，静止时跳过布局，最高60次每秒。地图和星河保留各自实例，以透明度过渡切换，隐藏场景暂停帧循环。未宣称所有设备恒定60 FPS；双指操作尚未手机实机测试。

省名以22px横排墨色文字、5px字距直接标在地图地理锚点上，无立牌、底板和引导线；避让偏移不超过8px，位置过密则留在省份选择列表。城市同样使用横排文字，旁边标注真实坐标圆点；景区改为横向名称和从真实点位垂直向上的引线。省界使用支持屏幕像素宽度的 LineSegments2，默认1.6px，选中渐变到2.4px，边界高程与地形同步插值。未选择时全国使用同一纹理口径，任意省份被选中都可显示其独立精细纹理；高亮色和省界强调仅跟随当前选择。

地址数据 public/data/scenic-addresses.json 通过原有已核验高德POI ID逐一取回，97条有地址，覆盖全部10个诗文目的地。联合景区明确为代表点地址，其余74个不猜测地址，显示所在市区和“详细地址待核验”。可用 scripts/refresh-scenic-addresses.mjs 更新。景区数据仍仅覆盖浙江，跨省高亮不会串用浙江景点，其他省份显示待接入。

体验审查落实：44px 地图标签与控制、键盘焦点、跳转内容入口、搜索标签、移动端布局、长列表延迟渲染、减少动态效果。浏览器检查全国到浙江及诗文阅读；布局自动测试覆盖密集标签与聚合优先级。仍可继续改善 URL 深链接与极端小屏幕体验。

城市标注来自 Natural Earth populated places：34 个省级地区的中心城市加深圳，共35个去重地名；一线城市采用北上广深口径。普通城市仅在其所属省份高亮时显示文字和圆点，全国视野隐藏。北京始终使用红色五角星；真实位置移出地图可用画面时转为明确的“北京方向”边缘提示，点击可前往，不伪装成当地坐标。城市文字与景区共同避让，44px点击区域，不与景区聚合。来源和概化坐标记录见 public/data/city-labels.json，生成脚本 scripts/build-city-labels.mjs。

地形从2.5倍起预载鼠标与视野周边省份，显式选择在飞入时即预载；最多2个后台预载，预载与选中共用加载任务。纹理只初始化一次并提前上传GPU，不再每次加载细节都重新上传共享全国纹理。地形与青绿纹理使用相同过渡进度，阻尼速率由9增至18；高亮颜色独立快速响应，未选中的省份高亮立即归零，避免旧地形退场时继续高亮。所有地形保留深度写入，省界不透明、无虚线、全国颜色为纯白，始终排在地形之后绘制，当前省界排在其他省界之后。首次冷加载仍受网络与GPU准备时间影响，加载中显示状态；失败可重选重试，不承诺零等待。

倾斜版图：相机统一使用 [0, -36, 28] 偏移（俯角约38°），包围盒适配采用同一视角投影，点击飞入和返回不会改变倾角。全国外缘添加石绿至深绿的浅侧壁，顶部跟随DEM混合高程，省际共边不重复挤出；保留原有青绿纹理、真实地势与白色省界。景区名称横排16px，纸色底、旧金细线；标签以点位为中心，只在正上方24–116px内选择高度，名称与竖线都参与碰撞检查，密集点通过原有聚合和名录继续访问。景点地面圆点按当前高程投影，移除旧立牌及旋转效果；缩放平移时景区标签与引线统一更新，避免CSS缓动拖离地面。
